{
  "Logging": {
    "LogLevel": {
      "Default": "Debug" // Set the default log level to Debug or lower
    },
    "Console": {
      "LogLevel": {
        "Default": "Debug" // Override for the console to ensure Debug logs are shown
      }
    }
  }
}